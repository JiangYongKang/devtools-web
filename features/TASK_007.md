### 任务目标

1. 生成随机 UUID，并提供解析、校验与多种分段或展示形式，方便复制与对照。
2. 纯前端，不请求后端。

### 任务要求

1. 非法输入有明确反馈；合法路径下展示稳定、可理解。
2. 可选能力（如 NIL、多种格式）是否在范围内由研发决定，DOC 写清即可。

### 任务约束

1. 禁止后端网络请求；不用 `src/services/*Api`。

### 实现位置

须在 `devtools-web/src/tools/uuid/` 下实现本任务全部代码；路由衔接见 DOC。

### 交付产物

1. 上述目录内实现。
2. `docs/DOC_007.md`。

### 注释规范

解析宽松度与随机源相关分支附简短中文说明。

### 验收标准

1. 无后端依赖即可完整使用。
2. DOC 与实现一致。
