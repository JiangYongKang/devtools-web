### 任务目标

1. 用户输入正则表达式与样本文本，在浏览器内展示匹配结果或高亮区间等可理解反馈。
2. 纯前端；不访问后端。

### 任务要求

1. 正则编译失败、无命中与有命中等状态区分清楚；宜考虑长文本或复杂表达式时的风险提示或限制（具体方案由研发定，DOC 记录）。
2. 用户输入不得以不安全方式渲染。

### 任务约束

1. 页面与逻辑分离：正则编译与匹配结果加工等须在 `src/tools/regex/` 内独立文件中实现并由页面引入；不在主 JSX 中长段堆砌与 DOM 无关的逻辑。
2. 禁止后端请求；不用 `src/services/*Api`。

### 实现位置

须在 `devtools-web/src/tools/regex/` 下实现本任务全部代码；路由衔接见 DOC。

### 交付产物

1. 上述目录内实现。
2. `docs/DOC_008.md`。

### 注释规范

与超时、步数或防抖等体验保护相关的逻辑简要中文说明。

### 验收标准

1. 无网络依赖即可完成主要流程。
2. DOC 与实现一致。
