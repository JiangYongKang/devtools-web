### 任务目标

1. 提供 HTML 源码的美化与单行压缩类能力，在浏览器内完成。
2. 不请求后端；不在本任务范围内承诺 XSS 消毒，边界在 DOC 写清。

### 任务要求

1. 交互清晰；错误与超大输入有预期行为说明（DOC）。
2. 展示用户内容时注意安全（避免将未处理 HTML 写入危险 API）。

### 任务约束

1. 禁止后端请求；不用 `src/services/*Api`。

### 实现位置

须在 `devtools-web/src/tools/html/` 下实现本任务全部代码；路由衔接见 DOC。

### 交付产物

1. 上述目录内实现。
2. `docs/DOC_009.md`。

### 注释规范

解析或压缩策略与兼容性假设处简要中文注释。

### 验收标准

1. 无后端依赖可用。
2. DOC 与实现一致。
