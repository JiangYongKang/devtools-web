### 任务目标

1. 提供 CSS 样式文本的排版整理与压缩，在浏览器内完成。
2. 纯前端，不访问后端。

### 任务要求

1. 选项与默认行为在界面或说明中可辨认；解析失败时有反馈。
2. 注释剔除、source map 相关保留等策略由研发与 DOC 约定。

### 任务约束

1. 页面与逻辑分离：CSS 排版与压缩等须在 `src/tools/css/` 内独立文件中实现并由页面引入；主组件不承载大块与 UI 无关的解析与变换代码。
2. 禁止后端请求；不用 `src/services/*Api`。

### 实现位置

须在 `devtools-web/src/tools/css/` 下实现本任务全部代码；路由衔接见 DOC。

### 交付产物

1. 上述目录内实现。
2. `docs/DOC_010.md`。

### 注释规范

压缩等级与风险相关分支简要中文说明。

### 验收标准

1. 无后端依赖可用。
2. DOC 与实现一致。
